1) Importare il file Dump20130701.sql nel proprio database, contenente i dati relativi ai prodotti.
2) I dati sono divisi in due tabelle, dove la prima, "art" contiene codice, descrizione, costo unitario, costo ordinazione
 ed eventualmente la quantità per cui vi è lo sconto, e l'aliquota di sconto relativi a ogni prodotto,
"movim" che contiene per ogni vendita registrata il codice del prodotto in questione, l'anno e il mese in cui è avvenuta la vendita, e la quantità venduta.
3)Lanciare il programma: si aprirà un'interfaccia grafica che permette di inserire un nuovo prodotto, inserire una nuova vendita o visualizzare le informazioni richieste,
 tra cui il clacolo dell'EOQ, il costo totale di magazzino e gli altri valori ricavati dal programma.